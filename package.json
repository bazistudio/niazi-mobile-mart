{
  "name": "niazi-mobile-mart-workspace",
  "version": "1.0.1",
  "private": true,
  "description": "Niazi Mobile Mart Desktop ERP & POS Workspace",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "build:web": "cross-env VITE_APP_SURFACE=web npm --prefix frontend run build",
    "build:desktop": "cross-env VITE_APP_SURFACE=desktop npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "lint": "npm --prefix frontend run lint"
  }
}
